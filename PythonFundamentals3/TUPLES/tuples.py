# immutable sequence of values (immutable - cant change the value inside the array)
tup = (1, 2, 4, 3, 5)
print(tup)
print(type(tup))
print(len(tup))
print(tup[2]) #index value

# we can also add multiple datatypes values in a tuple
tup1 = (1, 2.43, 4, "abc", 5)

 