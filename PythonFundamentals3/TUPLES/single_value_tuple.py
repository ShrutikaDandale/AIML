# we cant directly write a single value inside a tuple 
# if we write a single value then output will give the type of the value strored inside the tuple (ex- 1 = int, abc = str)
# so we give , after value
tup = (1,)
print(type(tup))
