# Ask the user for: Principal (P), Rate (R), Time (T). Convert all to floats and compute simple interest: SI = (P * R * T) / 100


p = float(input("enter p: "))
r = float(input("enter r: "))
t = float(input("enter t: "))
SI = (p * r * t)/100
print(SI)
